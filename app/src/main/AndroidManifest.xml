<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- "Save image" (long-press menu, in-reader image viewer) writes through MediaStore's
         scoped-storage API on Android 10+ (Q, API 29), which needs no permission at all. Below
         that, MediaStore inserts still require the legacy runtime permission, capped at 28 so
         it's not requested/declared at all on the versions where it'd be a no-op. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Needed on Android 11+ (package visibility) so ReaderSettingsSheet's dictionary-app picker
         can see every installed app that supports Android's standard ACTION_PROCESS_TEXT "Process
         text" intent. Without this queries declaration, PackageManager.queryIntentActivities
         silently returns an empty list for other apps' activities regardless of what's installed. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:name=".IshiReaderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.IshiReader"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic is on because the server this app talks to is a self-hosted
          Ishi-Read instance whose address the user supplies at runtime (often a bare LAN
          IP without TLS); see Ishi-Read's own README note about needing a reverse proxy
          for HTTPS. Tighten this with a network_security_config once a fixed, HTTPS-only
          deployment is the norm.
        -->

        <!-- IshiReaderApp supplies its own WorkManager Configuration (a custom WorkerFactory that
             injects PositionDao/NetworkModule into PositionSyncWorker), which requires turning off
             WorkManager's default auto-initializer; otherwise the default Configuration wins the
             race and PositionSyncWorker can't be instantiated. -->
        <!-- Lets "Copy" on the manga reader's long-press menu put the current page's image on the
             system clipboard as a content:// URI (ClipData.newUri): the clipboard framework needs
             a real content provider to grant the receiving app temporary read access, a plain
             file:// URI from cacheDir isn't accessible outside this app. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.IshiReader.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".reader.ReaderActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />

        <activity
            android:name=".audiobook.AudiobookPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />

        <service
            android:name=".audiobook.AudiobookPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
